import Icon from '../../web_modules/@material-ui/core/Icon.js'

import { styled } from '../../stitches.config.js'

export const StyledStartIcon = styled(Icon, {
	marginRight: '0.5rem'
})

export const StyledEndIcon = styled(Icon, {
	marginLeft: '0.5rem'
})
